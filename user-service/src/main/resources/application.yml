spring:
  application:
    name: user-service

  #DB configuration
  datasource:
    url: jdbc:mysql://localhost:3306/userDB
    username: root
    password: 12345678

  jpa:
    hibernate:
      ddl-auto: create-drop

    show-sql: true

    properties:
      hibernate:
        format_sql: true

#Eureka config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  servlet:
    context-path: /api/v1

  port: 9020